# Copy this to .env (and add .env to .gitignore) or set these as env vars
# in your host's dashboard (Vercel/Render project settings).

IPOGURU_API_KEY=
INDIANAPI_API_KEY=sk-live-5ijpTPTfFB4vp2wUjK5g429rjMGALhAtj50o6oKT

# Optional, defaults shown:
SYNC_INTERVAL_MINUTES=60
POST_LISTING_TRACK_DAYS=12

# Set to 1 only on a long-running host (Render web service / VM / Railway).
# Leave unset on Vercel -- use an external cron hitting POST /api/sync instead.
RUN_SCHEDULER=0
